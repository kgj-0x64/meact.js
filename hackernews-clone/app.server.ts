/// USERLAND server (imported by the framework in "./meact-framework/server/index.js")
/// ! name of the file must not be changed

// initialize server resources i.e. cache, DB and services
import "./server/bootstrap.server.ts";
